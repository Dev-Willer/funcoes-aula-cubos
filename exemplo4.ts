/*function soma (numero1: number, numero2: number) {
    return numero1 + numero2
}

console.log(soma(6, 9))*/

import { log } from "console"

/*const soma2 = (numero01: number, numero02: number) => {
    return numero01 + numero02
}

console.log(soma2(9,18));*/

function nomeCompleto (nome: string, sobrenome: string): string {
    return `${nome} ${sobrenome}`
}
console.log(nomeCompleto('Willer', 'Santos'));

